# Началната страница в конструктора

Къде е в панела: **Маркетинг → Страници**, редът „Начална (нова)“ (id 99).
Панелът се отваря на адреса на магазина, `c2wjn.cloudcart.net/admin`, не на
адреса на редизайна.
Адрес в магазина: **`/page/nachalna-nova`**

Страницата е изградена от 25 реда, точно в реда на сегашната начална страница.
Докато не бъде заложена като начална, тя живее на своя си адрес и нищо в магазина
не се променя.

---

## Кое може да се сменя от панела

| Клас на реда | Какво съдържа | Какво можеш да правиш |
|---|---|---|
| `bb-row-hero` | Карусел с 4 банера + числата отгоре | Качваш нови банери, сменяш реда им, слагаш линк на всеки |
| `bb-row-trust` | По една колона на гаранция | Пишеш текста, сменяш иконата, добавяш и махаш гаранции |
| `bb-row-marquee` | Списък с предимствата в бягащата лента | Пишеш, добавяш и махаш редове |
| `bb-row-head` | Два текстови блока: заглавие и подзаглавие | Пишеш какъвто текст искаш |
| `bb-row-rail` | Продуктова витрина с 10 продукта | Избираш кои продукти да се показват и в какъв ред |
| `bb-row-doverie` | Голямото число и текстът до него | Пишеш числото, текста и хапчетата под него |
| `bb-row-headline` | Центрирано заглавие на секция (три такива реда) | Пишеш какъвто текст искаш |
| `bb-row-doctors` | По една колона на лекар | Добавяш, махаш и пренареждаш лекари |
| `bb-row-bundle` | Пакет на месеца: текст + избран продукт + снимка | Сменяш продукта, текста и снимката всеки месец |
| `bb-row-forms` | По една колона на форма (капсули, перли…) | Пишеш текста, сменяш иконата и връзката, добавяш карти |
| `bb-row-tiles` | По една колона на плочка | Добавяш, махаш и пренареждаш плочки |
| `bb-row-founder` | Портретът и разказът на основателя | Сменяш снимката, етикетчето и целия текст |
| `bb-row-press` | Лентата с медиите | Пишеш надслова, добавяш и махаш издания, сменяш връзките |
| `bb-row-brand` | „Българска традиция“ върху снимка | Сменяш снимката от фона на реда, текста и бутона |
| `bb-row-faq` | Често задаваните въпроси | Пишеш, добавяш и махаш въпроси и отговори |
| `bb-row-ctas` | Двете големи карти най-долу | Сменяш снимките, текстовете и връзките; добавяш трета карта |
| `bb-row-bloghead` | Заглавието на блога + бутонът към него | Пишеш текста, сменяш надписа и адреса на бутона |
| `bb-row-blog` | Статиите от блога | Избираш колко статии и по колко в ред |

Редовете нямат номера тук нарочно: подредбата се сменя с влачене и всеки номер
щеше да остарее още първия път, когато преместиш нещо. Ред се разпознава по
класа си, който стои в настройките му.

Освен това **на всяко място** можеш да добавиш нов ред и в него: Текст,
Заглавие, Банер, Бутон, Видео, Разделител, Снимка, Продуктова витрина,
Карусел или Статии. Редовете се местят с влачене, така че цялата подредба на
страницата е в твои ръце.

### Банерите в героя

Всеки слайд има две снимки: **широка за компютър** и **висока за телефон**.
Качваш и двете, за да изглежда добре навсякъде. Размерите на сегашните:
компютър ~2033×773, телефон ~1070×1470.

### Лентата с гаранции

Една гаранция = една колона с Текст блок, а в него един абзац. Иконата се
избира с клас на абзаца:

```html
<p class="ico-truck">Безплатна доставка над 50 €</p>
```

Готовите икони: `ico-shield` (щит), `ico-truck` (доставка), `ico-lab`
(лаборатория), `ico-guarantee` (глобус), `ico-leaf` (листо), `ico-heart`
(сърце). Абзац без такъв клас излиза без икона.

Цветът на квадратчето не се избира - идва от мястото на колоната и се върти в
четирите цвята на марката. Пета гаранция минава на нов ред и започва цветовете
отначало.

### Бягащата лента

Съдържанието е **обикновен списък** в Текст блок - един ред от списъка е едно
предимство. Лентата ги подрежда сама, слага розова точка пред всяко и ги върти
без край.

Ако в началото на реда има **удебелена** дума, тя излиза като розово етикетче
вместо точката - така е направено „НАУКА“ на първия ред.

Пиши истинските знаци (— → € ≈), а не HTML кодове: кодове като `&mdash;` излизат
като текст в лентата.

### Доверие, голямото число

Редът има две колони:

- **лявата**: един абзац с класа `stat-big` - в него се пише числото, а знакът
  „+“ се прави с бутона за горен индекс: `110K<sup>+</sup>`
- **дясната**: заглавие 3, един абзац и **списък** - всеки ред от списъка
  излиза като бяло хапче с розова точка

Розовият фон е част от оформлението и не се пипа от панела.

### Формите (капсули, перли, таблетки, сашета)

Заглавието е отделен ред с класа `bb-row-headline`, а картите са ред с класа
`bb-row-forms` - една карта е една колона с един Текст блок в нея:

```html
<h3 class="ico-pearls">Пробиотични перли</h3>
<p>Дъвчащи перли с млечен и натурален шоколад без захар.</p>
<p class="who"><a href="/category/perli">Деца и възрастни</a></p>
```

- иконата над заглавието се избира с клас на `h3`: `ico-capsule`, `ico-pearls`,
  `ico-tablet`, `ico-sachet` (важат и шестте от лентата с гаранции)
- **последният абзац** трябва да е с класа `who`: той става долната лента със
  стрелката, а връзката в него прави **цялата карта** кликаема
- цветното петно горе се върти по мястото на колоната, както при гаранциите

### Основателят

Редът има две колони:

- **лявата**: Текст блок с един абзац с класа `stamp` (етикетчето върху
  снимката) и под него Снимка
- **дясната**: един Текст блок с целия разказ

**Дългият разказ се крие зад бутон „Прочети повече“.** Правилото е просто и се
вижда в редактора: каквото стои **между две хоризонтални черти** се скрива.
Хоризонталната черта се слага с бутона за разделител в редактора.

```
надслов, заглавие, двата абзаца, цитатът
──────────────  ← първата черта
дългият разказ
──────────────  ← втората черта
подписът (винаги се вижда)
```

Ако сложиш само една черта, всичко след нея се крие. Ако не сложиш черта,
текстът е обикновен - **това важи за всеки Текст блок на страницата**, не само
тук.

Подписът е един абзац с класа `sign`: наклоненият текст в началото става
розовото кръгче с инициалите, а текстът в `span` - длъжността.

### Медиите за нас

Редът е с класа `bb-row-press` и в него **първата колона е надсловът**,
**последната е връзката отдолу**, а всяка колона между тях е по едно издание:

```html
<!-- първата колона -->
<p class="press-head"><strong>Медиите за нас</strong>
<span>19+ реални публикации · Виж всички →</span></p>

<!-- всяка следваща колона: едно издание -->
<p class="outlet serif"><a href="/article/handle-na-statiyata">EVA</a>
<span>Награда</span></p>

<!-- последната колона -->
<p class="press-cta"><a href="/page/mediite-za-nas">Виж пълния списък →</a></p>
```

- класът `serif` прави името с наклонения шрифт; без него е обикновеният
- връзката в изданието прави **цялата картичка** кликаема
- ново издание = нова колона, вмъкната **преди последната**

### Българска традиция

Тук снимката **не е блок, а фон на самия ред**: отваряш настройките на реда,
избираш „Снимка“ за фон и качваш друга - това е единственото място на
страницата, където снимката се сменя оттам.

В колоната стои Текст блок (надслов, заглавие 3 и абзац) и Бутон. Върху
снимката пада светло було, за да се чете текстът; то е част от оформлението.

### Долните призиви

Като плочките „За всеки етап“: една карта е една колона със **Снимка** (сложи ѝ
линка - тя прави цялата карта кликаема) и **Текст** върху нея:

```html
<p class="cta-tag">BACTOLOGY · Лаборатория</p>
<h3>Науката зад<br><span class="accent">Bactology.</span></h3>
<p>Български щамове, тествани в нашата лаборатория.</p>
<p class="cta-btn">Прочети повече</p>
```

Абзацът с класа `cta-btn` изглежда като бутон, но не е връзка - връзката е
снимката. Трета карта = трета колона.

### Често задавани въпроси

Редът има две колони:

- **лявата**: Текст блок със заглавието на секцията и абзац, а накрая абзац с
  класа `faq-link`, в който връзката става хапчето „Пиши ни директно“
- **дясната**: Текст блок с въпросите и отговорите:

```html
<h3>След колко време ще усетя резултат?</h3>
<p>Първи измерими промени се установяват след около 14 дни…</p>
<h3>Какво са пробиотиците и как действат?</h3>
<p>Пробиотиците са натурални култури…</p>
```

**Въпрос е Заглавие 3** - всичко под него до следващото Заглавие 3 е отговорът.
Заглавие 2 не се брои за въпрос, затова заглавието на секцията вляво си остава
заглавие.

Показват се първите **5** въпроса, останалите излизат с бутона „Виж още“.
Отворен стои по един отговор наведнъж.

### Продуктовата витрина

В настройките на блока има поле за продукти. Отметката **„Слайдер“** трябва да
остане включена, за да са карти в лента, както е сега. Изключиш ли я, същите
продукти се подреждат в мрежа.

### Лекарите

Един лекар = една колона в реда. За да добавиш нов, копираш колона и сменяш
съдържанието ѝ. За да махнеш лекар, триеш колоната му. Колоните се пренареждат
с влачене и сами се пренасят на нов ред, ако станат повече от три.

В колоната има три блока, точно в този ред:

1. **Снимка** - портретът, показва се като кръгче
2. **Заглавие** - името, с титлата
3. **Текст** - останалото, в тази подредба:
   - първи абзац: специалността (излиза в розово)
   - втори абзац: клиниката или университетът
   - **цитат** (бутонът за цитат в редактора): това, което стои винаги видимо
   - каквото напишеш **след цитата**: визитката, която се разгъва при посочване

Ако не сложиш цитат, картата просто няма разгъваща се част.

### Пакет на месеца

Редът има две колони:

- **лявата**: Текст блок със заглавието, абзаца и точките (обикновен списък),
  а под него **Продуктова витрина с точно един продукт** - от него идват
  цената и бутонът, така че цената винаги е актуалната
- **дясната**: Снимка

Смяната на пакета е: смени продукта в витрината, смени текста, смени снимката.

### Плочките „За всеки етап“

Една плочка = една колона. В нея има **Снимка** (сложи ѝ линка, за да е
кликаема цялата плочка) и **Текст** със заглавие 3 и един абзац. Първата
колона е голямата плочка вляво; за да смениш коя е голямата, преместиш я на
първо място.

За розовата дума в заглавието се използва `<span class="accent">дума</span>`.

### Статии от блога

Заглавната част е Текст блок, а „Виж всички статии“ е Бутон - и двете се
пишат в панела. Самите карти са блокът **Статии**: той сам взима **най-новите**
публикации на блога, затова нова статия влиза в началната страница без намеса.
В настройките му се задава колко статии да се показват и по колко в ред
(сега: 3 и 3).

Корицата на всяка карта е снимката на статията от **Блог » Статии**. Смениш ли
я там, сменя се и тук.

⚠️ Разлика спрямо преди: дотогава в началната стояха три предварително избрани
статии. Сега стоят трите най-нови. Ако някоя статия трябва да е в началната
завинаги, тя се закача с отделен ред (Банер със снимка и линк), а не през
блока Статии - той не позволява ръчен избор.

---

## Останалите редове: блок „Код“ с един ред вътре

Секциите, които са направени специално за този магазин (лекарите, анимацията на
капсулата, наръчникът за избор, наградите, отзивите, ЧЗВ…), не са текст, който
се пише, а работещи части от дизайна. Затова в конструктора всяка от тях е един
блок **Код** със само един ред вътре, например:

```
<!-- bb:doctors -->
```

Този ред казва „тук постави секцията с лекарите“. Можеш да:

- **преместиш** секцията, като преместиш реда ѝ нагоре или надолу,
- **махнеш** я, като изтриеш реда,
- **сложиш нещо между** две секции, като вмъкнеш нов ред.

Каквото и да е друго в блок Код се показва като обикновен HTML.

### Пълен списък на маркерите

| Маркер | Секция |
|---|---|
| `<!-- bb:hero -->` | Кампаниен слайдер (готовият, с вградените банери) |
| `<!-- bb:stats -->` | Числата върху банера (110k+ клиенти…) |
| `<!-- bb:featured -->` | Продукти на фокус (готовата секция със заглавието) |
| `<!-- bb:doctors -->` | Какво казва медицината (готовата, с вградените лекари) |
| `<!-- bb:bundle -->` | Пакет на месеца (готовият, с вградения текст) |
| `<!-- bb:capsule -->` | Анимацията на капсулата |
| `<!-- bb:categories -->` | Плочки с категории (готовите) |
| `<!-- bb:finder -->` | Наръчник за избор (тестът) |
| `<!-- bb:reviews -->` | Отзиви на клиенти |
| `<!-- bb:awards -->` | Награди (слайдер) |
| `<!-- bb:stories -->` | Истории на клиенти |

---

## Клас на реда

Всеки ред има поле **„Клас“**. Класът свързва реда с оформление, което е
описано в кода на дизайна. Използваните тук са:

- `bb-row-hero` прави числата да стоят върху банера,
- `bb-row-trust` подрежда гаранциите в бяла лента с икони,
- `bb-row-marquee` превръща списъка в бягаща лента,
- `bb-row-doverie` дава розовия фон и голямото число,
- `bb-row-forms` прави от всяка колона бяла карта с икона,
- `bb-row-founder` слага портрета вляво и разказа вдясно,
- `bb-row-press` прави от колоните бели картички с изданията,
- `bb-row-brand` центрира текста върху снимката на реда,
- `bb-row-faq` подрежда заглавието вляво и въпросите вдясно,
- `bb-row-ctas` прави от колоните големи карти със снимка,
- `bb-row-head` подрежда заглавието и подзаглавието в две колони,
- `bb-row-rail` центрира продуктовата лента в ширината на магазина,
- `bb-row-headline` центрира заглавие на секция,
- `bb-row-doctors` превръща всяка колона в картичка на лекар,
- `bb-row-bundle` дава фона и оформлението на „Пакет на месеца“,
- `bb-row-tiles` подрежда колоните в плочките „За всеки етап“,
- `bb-row-bloghead` слага заглавието вляво и бутона вдясно,
- `bb-row-blog` кара блока „Статии“ да рисува картите на блога от дизайна.

Ако искаш собствено оформление, пишеш свой клас на реда и стиловете за него в
**Дизайн → Персонализиран CSS/JS**, между тези два реда:

```css
/* bb:css:start */
.моят-клас { ... }
/* bb:css:end */
```

Само това, което е между двата маркера, влиза в редизайна. Промяната се вижда
до 30 секунди, без ново качване на кода.

---

## Как се заменя началната страница

Когато страницата придобие желания вид, тя се залага като начална. Тогава
`/` показва нея, а старата кодирана версия остава в кода като резерва.

**Връщане назад, ако нещо не е наред:** добави нов ред с блок Код, в който
пише само `<!-- bb:off -->`. Магазинът веднага се връща към кодираната начална
страница. Махнеш ли реда, композицията се връща. Има и втори път: деактивиране
на страницата от панела.

---

## Продуктовите страници

Проверено на 2026-08-07 срещу админ API-то на магазина:

- **Снимките** идват направо от продукта. Смениш ли основната снимка или реда
  на снимките в панела, галерията на страницата се сменя. Няма зашити снимки в
  кода на продуктовите страници.
- **Категорията** идва от продукта. Тя пълни трохите над заглавието. Смениш ли
  категорията, се сменя и пътеката.
- **Описанието** вече идва от полето „Описание“ на продукта, с HTML в него.

### Какво се промени при описанията

До 2026-08-07 текстовете на **24 продукта** живееха в кода
(`app/lib/product-descriptions.ts`) и застъпваха написаното в панела, тоест
редакция в панела не се виждаше никъде. Същите текстове са пренесени в полето
„Описание“ на съответните продукти и файлът е премахнат. Оттук нататък панелът
е единственият източник.

При пренасянето се запазват и двете неща, които бяха в старото описание:
видеото (показва се най-горе) и решетката с научните проучвания.

Две разлики, които се виждат на тези 24 страници:

1. Редовете с емоджи-водачи вече излизат в лентата „Основни ползи“, вместо да
   стоят като обикновен текст в описанието. Така изглеждат като при останалите
   продукти.
2. Решетката с научните проучвания вече се показва. Тя стоеше в описанието и
   преди, но за тези продукти не се рисуваше. Ако не я искаш при някой продукт,
   изтриваш я от описанието му в панела.

Старите описания са запазени и връщането е един скрипт:
`scratchpad/restore_descriptions.py DOMAIN TOKEN descriptions-original.json`.

⚠️ Същото пренасяне трябва да се направи и на реалния магазин в деня на
прехвърлянето. Скриптът е `scratchpad/descriptions_to_panel.py` и работи срещу
всеки магазин, стига да има админ токен.
