/**
 * Адресът, на който живеят действията с количката.
 *
 * Нарочно НЕ е `/cart`.
 *
 * Диспечерът на Nova (`nova-dispatch.js`) държи списък `RESERVED_PREFIXES` -
 * пътища, които не стигат до storefront worker-а, а се проксират към
 * платформата: `/admin`, `/checkout`, `/api/sf`, `/cdn/` и други. Планът е
 * `/cart` да влезе в този списък, за да рисува количката платформата - тогава
 * подаръците от кръстосаните оферти и правилата за количката ще важат, вместо
 * да ги пресмятаме наново отстрани.
 *
 * Но `/cart` беше и адресът, на който Nitrogen праща всяко действие:
 * добавяне, промяна на количество, премахване, промо код. Резервира ли се
 * пътят, тези заявки отиват при платформата, която не ги разбира, и всеки
 * бутон в магазина спира тихо.
 *
 * Затова действията се местят тук предварително. Промяната е безобидна сама по
 * себе си и прави резервацията безопасна за пускане по всяко време - двете
 * страни спират да чакат една друга.
 *
 * Държи се в константа, а не като низ на 16 места, за да не се разминат при
 * следващото местене.
 */
export const CART_ACTION = '/cart-actions';
