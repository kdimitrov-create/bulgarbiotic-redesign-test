import {HeroBannerSlider} from './HeroBannerSlider';

/**
 * Home hero — the full-width campaign banner slider (client choice, "Variant B").
 * The previous "Българската биология" text hero + floating badges are preserved
 * in git history if we ever want to bring them back.
 */
export function Hero() {
  return <HeroBannerSlider />;
}
